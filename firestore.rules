rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // ログインしている全ユーザーに全ての読み書きを許可（デバッグ用）
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
